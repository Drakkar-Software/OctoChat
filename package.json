{
  "name": "octochat",
  "version": "0.0.0",
  "private": true,
  "description": "OctoChat — a universal (web + native) Expo sticker editor, built as a pnpm monorepo.",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "infra:up": "docker compose up -d nats",
    "infra:down": "docker compose down",
    "whistlers": "QUEUE_URL=nats://localhost:4222 node infra/whistlers-sse.mjs",
    "dev": "concurrently --names server,whistlers --prefix-colors cyan,magenta \"NATS_URL=nats://localhost:4222 pnpm --filter @octochat/server dev\" \"pnpm whistlers\"",
    "dev:server": "pnpm --filter @octochat/server dev",
    "start": "pnpm --filter @octochat/mobile start",
    "web": "pnpm --filter @octochat/mobile web",
    "web:package": "pnpm --filter @octochat/desktop export",
    "android": "pnpm --filter @octochat/mobile android",
    "ios": "pnpm --filter @octochat/mobile ios",
    "desktop": "pnpm --filter @octochat/desktop dev",
    "desktop:package": "pnpm --filter @octochat/desktop package",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test"
  },
  "dependencies": {
    "@drakkar.software/whistlers": "^0.6.0"
  }
}
