/** Identifier helpers — re-exported from @drakkar.software/starfish-protocol
 *  (moved out of dk-spaces-sdk's proxy layer in 0.31).
 *  (octospaces 0.16 renamed `roomSlug` → `slugify`; OctoChat keeps its room name.) */
export { randomId, slugify as roomSlug } from '@drakkar.software/starfish-protocol';
