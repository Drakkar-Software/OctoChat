# Screenshots

README assets.

- `app.png` — workspace / chat view ("Select a room" empty state). **Add this
  file** (the chat-UI screenshot) here; the root `README.md` references it.

The welcome screen shot is reused from
`apps/mobile/public/screenshots/welcome-wide.png`.
